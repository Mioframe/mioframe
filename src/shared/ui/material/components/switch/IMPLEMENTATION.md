# Switch implementation

Artifact revision: 2026-08-14T14:00:00.000Z
Status: complete
ARCHITECTURE.md reference: `src/shared/ui/material/components/switch/ARCHITECTURE.md`
ARCHITECTURE.md revision: 2026-08-14T14:00:00.000Z
Revision summary: Added real touch-input proof for the `presentation` and `presentation-composition` scenarios (`MDSwitch.browser.spec.ts`, `test.use({ hasTouch: true })`, `page.touchscreen.tap`), closing ARCHITECTURE.md's outstanding "must be verified under both mouse and touch input" risk for the installed 2.7.4 renderer. No production `MDSwitch.vue` change; existing mouse proof is unchanged.

Remaining blockers: none
Required return family: none
Required return stage: none
Architecture deviations: none
Migration readiness: ready

## Implemented passes

1. **Controlled-state correction (`MDSwitch.vue`):** replaced the `@change`-derived `onChange` handler with `onBeforeinput`, bound directly on the `m3e-switch` template root (explicit template wiring, not `$attrs`). The handler runtime-narrows `event.target` with `instanceof M3eSwitchElement` (existing pattern, unchanged), no-ops (no `preventDefault()`, no emit) when `props.presentation` is true, otherwise calls `event.preventDefault()` before computing `!event.target.checked` and emitting `update:selected` with that intended-next value. Deliberately does not add a wrapper-level `disabled` check inside the handler — the renderer's own click handler already blocks `beforeinput` dispatch before `disabled`, per ARCHITECTURE.md's "Forbidden" list, so a redundant check would only mask a real renderer regression. No wrapper-local `ref` shadow of `selected` and no `watch`/`watchEffect` repair path were added; the existing one-way `:checked="props.selected"` binding remains the only writer of `checked`. Updated the `selected` prop and `update:selected` emit TSDoc to describe the one-directional, pre-mutation-intent contract.
2. **Private renderer typing (`src/shared/ui/material/m3eSwitch.d.ts`):** replaced `onChange?: (event: Event) => void` with `onBeforeinput?: (event: Event) => void`, documenting that the installed `@m3e/web@2.7.4` renderer dispatches `beforeinput` as a plain `Event`, not the ambient DOM lib's `InputEvent`.
3. **Switch-owned Vitest `ElementInternals` shim:** removed the global `HTMLElement.prototype.attachInternals` polyfill and its `MockElementInternals` class from `src/setupVitest.ts`. Added `src/shared/ui/material/components/switch/MDSwitch.testUtils.ts`, exporting `installSwitchElementInternalsShim()`, which installs the same minimal `MockElementInternals` surface only when `attachInternals` is absent (preserving any pre-existing implementation) and returns a teardown function that removes the installed property. `MDSwitch.test.ts` calls it from `beforeAll`/restores it from `afterAll`. Before deleting the global shim, confirmed no other current Vitest suite depends on it: inspecting the installed `@m3e/web` renderer bundle shows only the Switch entry (`@m3e/web/dist/switch.js`) references `FormAssociated`/`internals` machinery among the three `m3e-*` elements Mioframe currently selects (`config/vueCustomElements.ts`: `m3e-button`, `m3e-loading-indicator`, `m3e-switch`); Button and Loading Indicator reference neither. A full unrestricted-scope `pnpm verify --full --only unit-tests` run (see "Stage verification") confirms no regression traceable to the shim removal.
4. **Browser proof relocation:** created `src/shared/ui/material/components/switch/MDSwitch.browser.spec.ts`, migrating every still-valid assertion from the legacy `tests/e2e/storybook/md-switch-family.spec.ts` (click/Space/Enter activation, accessible name via `aria-labelledby`/`aria-label`, disabled activation/focus-order blocking, Tab-order unreachability for `disabled`/`presentation`, presentation pointer-unreachability and accessibility-tree hiding, 48×48dp target-hit, host-attribute-boundary rejection), rewritten for the corrected single-intent-per-activation contract, plus two new tests (rejected-intent non-mutation; presentation-composition pointer pass-through). Deleted `tests/e2e/storybook/md-switch-family.spec.ts` and removed the `switch family behavior` entry from `scripts/lib/storybookBehaviorRisk.mjs` (removed, not replaced, per current `develop` Storybook S2 policy).
5. **Component-contract proof (`MDSwitch.test.ts`):** rewrote the controlled-selection tests around a `dispatchBeforeinput` helper that dispatches a simulated cancelable `beforeinput` Event and returns it for `defaultPrevented` inspection. New/updated tests: single emission with the correct pre-mutation-negated value (two cases, `selected: false`→`true` and `selected: true`→`false`); rejected-intent non-mutation (emitted value never written back to `selected`, rendered `checked` stays unchanged); disabled non-emission proven through a real `click` Event dispatch relying on the renderer's own internal click-guard (no wrapper-level `disabled` check added, matching ARCHITECTURE.md's "Forbidden" list); `presentation` non-emission with `defaultPrevented` asserted `false`; the host-attribute-boundary duplicate-listener test extended to cover `onChange` alongside `onBeforeinput`/`onClick`. Removed the prior `change`-derived assertions superseded by this revision. All 16 tests pass.
6. **Storybook composition fixtures (`MDSwitch.stories.ts`):** added `RejectedIntent` (a switch pinned at `selected: false` whose handler only records the emitted intent, never writing it back) and `PresentationComposition` (a fixture-owned wrapping element with its own `role="switch"`/`aria-checked` and click handler, a local `selected`/`disabled` ref pair, and a decorative `MDSwitch presentation :selected :disabled` — since `pointer-events: none` makes the renderer itself unreachable, a real click at its visible coordinates must land on the wrapping owner instead). Reproduces no `AppUpdateSettings`/`SettingsSwitchListItem` product logic. **Fixed a genuine pre-existing `TargetHitArea` fixture defect surfaced by the correction:** that story never bound `:selected` or wrote `update:selected` back to a local ref; under the prior `change`-derived (post-mutation) contract this accidentally still toggled `aria-checked` because the renderer had already self-mutated `checked` before the adapter's handler ran, but under the corrected `beforeinput`/`preventDefault()` contract that mutation is genuinely blocked at the source, so the fixture would never show a toggled state again unless it completed the controlled round trip. Added a local `selected` ref, bound `:selected="selected"`, and updated `onUpdateSelected` to write the emitted value back — the real-browser proof this exposed (see "Stage verification") is what surfaced the defect.
7. **`config/vueCustomElements.ts` / `eslint.config.test.ts` / `config/vueCustomElements.test.ts`:** already registered `m3e-switch` from the prior implementation pass; inspected, unaffected by the `beforeinput` correction, no change needed.
8. **Real touch-input proof for `presentation` (`MDSwitch.browser.spec.ts`):** added a `test.describe('touch input', ...)` block scoped with `test.use({ hasTouch: true })`, containing two new tests that reuse the existing `presentation` and `presentation-composition` stories/fixtures unchanged and reproduce the same two mouse-path tests' assertions via `page.touchscreen.tap(x, y)` (Playwright's CDP-backed touch API, requiring `hasTouch: true`) at the same raw bounding-box coordinates used by the mouse tests — raw coordinates, not `locator.tap()`, because the decorative renderer's `pointer-events: none` fails `locator`-based actionability the same way it already rules out `locator.click()` for the mouse-path tests. The first test confirms a real touch at the decorative `presentation` switch's own visible location does not mutate the renderer-owned `checked` and leaves `aria-hidden`/`tabindex` unchanged; the second confirms a real touch on the decorative region in the `presentation-composition` fixture produces exactly one owning-fixture action, that the owner's resulting state flows back into the rendered `checked` through the existing `selected` prop, and that the decorative renderer's presentation semantics stay unchanged throughout. No production `MDSwitch.vue` change: touch proof confirmed the existing `pointer-events: none` suppression already generalizes correctly to touch input under the installed 2.7.4 renderer. Closes ARCHITECTURE.md's "Risks" bullet requiring mouse-and-touch revalidation after a renderer update.

## Public API implemented

- Canonical export: `MDSwitch` from `@shared/ui/material` (unchanged).
- Props: `selected` (`boolean`, default `false`, controlled one-directionally via `update:selected`); `disabled` (`boolean`, default `false`); `presentation` (`boolean`, default `false`, Mioframe composition extension). No public prop surface change from the prior revision — only the internal interaction mapping changed.
- Slots: none.
- Emit: `update:selected(value: boolean)`, now derived from the renderer's cancelable `beforeinput` intent (computed pre-mutation), never from `change`/`input`. Never fires while `presentation` is true; never reachable while `disabled` is true.
- Root: one semantic `m3e-switch`; no wrapper element, no exposed imperative API.
- Host boundary: `inheritAttrs: false`; only merged `class`/`style`, `id`, `title`, `data-*`, `aria-label`, and `aria-labelledby` reach the host. No consumer `beforeinput` or `change` listener can attach to the renderer host (proven in both `MDSwitch.test.ts` and `MDSwitch.browser.spec.ts`).

## Tokens and renderer mappings

No `components/switch/tokens.css` file and no `docs/token-api.md` change, matching ARCHITECTURE.md's zero-token selection (unchanged from the prior revision).

Renderer mapping implemented exactly as ARCHITECTURE.md's "Renderer mapping and gaps" table selects:

- `selected` → renderer `checked` Boolean property, one-directionally (prop → renderer only). `update:selected` is derived from the renderer's own cancelable `beforeinput` (confirmed in the installed `2.7.4` compiled source: `if (this.dispatchEvent(new Event('beforeinput', { bubbles: true, cancelable: true }))) { this.checked = !this.checked; ... }`), intercepted with `event.preventDefault()` before the renderer's mutation branch can execute, so `checked` only ever changes via the `:checked="props.selected"` binding.
- `disabled` → renderer `disabled` Boolean property (unchanged). The renderer's own click handler blocks its internal click-to-toggle and the `beforeinput` dispatch itself before either can occur while disabled, confirmed at the component-contract level this revision by dispatching a real `click` Event and asserting no emission (previously only real-browser proof covered this).
- `presentation` → wrapper-owned `tabindex="-1"`, `aria-hidden="true"`, host CSS `pointer-events: none` (unchanged), plus the `beforeinput` handler's no-op guard (new: previously the `change`-derived handler's `presentation` guard was the only defense; now it is explicit defense-in-depth alongside the unreachable-by-construction suppression attributes).
- No `icons`, `name`, `value`, or drag-to-toggle surface is exposed or wired (unchanged).
- Private typing seam (`m3eSwitch.d.ts`): `onBeforeinput?: (event: Event) => void` replaces `onChange?: (event: Event) => void`, documenting the plain-`Event` (not `InputEvent`) dispatch type.

## Dependencies

- Material foundation: supplies the `--md-sys-color-*`/`--md-sys-shape-*` roles the renderer consumes directly; not an official component-family dependency (unchanged).
- `@m3e/web@2.7.4` (`@m3e/web/switch`): private renderer boundary; `M3eSwitchElement` provides package-derived glue for `checked`/`disabled` typing and the `instanceof` runtime-narrowing target for the `beforeinput` handler.
- Dependency queue: none. No other Material family is composed by or required by Switch at this stage (`MDListItem` composition remains migration-stage work).

## Component-owned proof

- `MDSwitch.test.ts` (16 tests, passing): defaults, explicit/false Boolean-property mapping, controlled `checked` re-assertion on prop change, the `beforeinput`-derived `update:selected` intent (single emission, correct negated value for both starting states, `event.defaultPrevented` confirmed true), rejected-intent non-mutation, disabled non-emission via a real `click` Event relying on the renderer's own guard, `presentation` full-suppression and its `beforeinput` no-op (`defaultPrevented` confirmed false), and the complete host-attribute allow-list/rejection matrix including rejection of a duplicate consumer `beforeinput`/`change`/`click` listener.
- `MDSwitch.testUtils.ts`: the Switch-owned `attachInternals()` compatibility shim, install/teardown only, no assertions of its own (exercised indirectly by every `MDSwitch.test.ts` mount).
- `config/vueCustomElements.test.ts` / `eslint.config.test.ts`: pre-existing `m3e-switch` coverage from the prior implementation pass, unaffected and re-verified.
- `MDSwitch.browser.spec.ts` (10 tests, passing; owner-local, replacing the legacy central `tests/e2e/storybook/md-switch-family.spec.ts`, which is deleted, and its `scripts/lib/storybookBehaviorRisk.mjs` "switch family behavior" registry entry, which is removed): click/Space/Enter activation each producing exactly one public intent through the real renderer `beforeinput`/`preventDefault` lifecycle; accessible name via `aria-labelledby`/`aria-label` plus disabled activation/focus-order blocking — the disabled-activation assertion does not call the forbidden `disabled.click({ force: true })` (which would bypass the very actionability contract the proof exists to exercise); it reads the disabled switch's bounding box and performs an ordinary `page.mouse.click` at its own rendered coordinates, then re-asserts `aria-checked` stays `'false'`, matching the `page.mouse.click`-at-coordinates pattern this same file already uses for `presentation`, target-hit, and host-attribute-boundary proof; rejected-intent non-mutation (a real click reports the intent but leaves `aria-checked` unchanged because the fixture never writes it back); Tab-order unreachability for `disabled`/`presentation`; presentation pointer-unreachability and accessibility-tree hiding; presentation-composition pointer pass-through (a real click at the decorative switch's own visible coordinates reaches the fixture owner's action, the owner's state updates, and that state flows back into the rendered `checked`, while the decorative renderer never independently toggles); 48×48dp expanded target-hit confirmation; host-attribute-boundary rejection at the actual rendered element; **added this revision**, a `touch input` describe block (`test.use({ hasTouch: true })`) with two tests reusing the same `presentation`/`presentation-composition` stories and reproducing the equivalent mouse-path assertions via real `page.touchscreen.tap` input at the same raw bounding-box coordinates: presentation non-mutation/non-reachability under touch, and presentation-composition pointer pass-through under touch (exactly one owning-fixture action per touch activation, resulting state flowing back into `checked`, presentation semantics unchanged).
- `tests/e2e/visual/shared-ui/md-switch.spec.ts` (1 test, passing, unchanged): the existing baseline remains valid — this revision changes only the interaction-intent mapping, no rendered visual state.

Operator visual status: no-reported-defect. Automated proof does not claim subjective Material or renderer-motion acceptance.

## @m3e/web 2.7.4 compatibility revalidation

The installed Switch type and plain-`Event` `beforeinput` contract preserve the existing private adapter seam. The `NativeLabelAssociation` Storybook fixture and owner-local browser proof show that both documented native-label forms still expose an empty accessible name in Chromium, so M3E-004 remains active and explicit ARIA naming remains the selected backstop. Pressed, ripple, and state-layer implementation changes stay renderer-private; no wrapper synthesis or public API change is introduced.

## Stage verification

Verification from the prior revision (`2026-08-11T07:30:01.000Z`), unaffected by this revision's proof-only correction:

- `pnpm verify --only type-check` — passed (run twice, before and after the `TargetHitArea` story fix).
- `pnpm verify --only eslint --files <all touched files>` — passed (one `jsdoc/require-param`/`jsdoc/require-returns` warning on `MDSwitch.test.ts`'s `dispatchBeforeinput` helper, fixed by adding complete JSDoc; rerun clean).
- `pnpm verify --only format --files <all touched files>` — passed.
- `pnpm verify --only unit-tests --files src/shared/ui/material/components/switch/MDSwitch.vue src/shared/ui/material/components/switch/MDSwitch.test.ts src/shared/ui/material/components/switch/MDSwitch.testUtils.ts src/shared/ui/material/components/switch/index.ts src/shared/ui/material/m3eSwitch.d.ts src/shared/ui/material/index.ts src/setupVitest.ts` — passed (16/16).
- `pnpm verify --full --only unit-tests` (required once per ARCHITECTURE.md, to confirm no suite depended on the removed global `attachInternals` shim) — after one unrelated fix (see below), passed: 330 test files, 4457 tests passed, 0 failed. The first run (before the fix) showed 3 failed files / 65 failed tests, all inside `scripts/lib/storybookBehaviorRisk.test.mjs`/`scripts/verify*.test.mjs`, none referencing `attachInternals`/`ElementInternals`/Switch/DOM at all. Root cause: two pre-existing, unrelated stale entries in `scripts/lib/storybookBehaviorRisk.mjs` predating this session — a duplicate object key silently dropped the "dialog form fallback focus" scenario's real `specs` value in favor of `focusIndicator.spec.ts`, and the "shared focus-indicator integration" scenario still referenced the already-deleted `tests/e2e/storybook/md-button-family.spec.ts` (both stale relative to `docs/testing/migration-plan.md`'s already-documented S2-D/S2-E completed state, and neither exists on disk). This fail-closed the Storybook behavior scenario registry for the **entire project**, blocking my own required `storybook-behavior` proof for `MDSwitch.browser.spec.ts` too, not just the unrelated Button/Dialog scope. Since `docs/testing/migration-plan.md` and the resolver's own pinned test expectations unambiguously specify the correct target state, corrected `scripts/lib/storybookBehaviorRisk.mjs`: pointed "shared focus-indicator integration" at `focusIndicator.spec.ts` and removed the stale "dialog form fallback focus" entry entirely. Re-ran `pnpm verify --only unit-tests --files scripts/lib/storybookBehaviorRisk.mjs` (passed) and `pnpm verify --only format --files scripts/lib/storybookBehaviorRisk.mjs` (passed) before the full rerun above. This fix is Button/Dialog-family registry debt, not a Switch architecture or public-contract change; it is recorded here only because it blocked required Switch verification.
- `pnpm verify --only visual --files src/shared/ui/material/components/switch/MDSwitch.vue src/shared/ui/material/components/switch/MDSwitch.stories.ts` — passed (212/212, run twice — once before and once after the `TargetHitArea` fix — both green, confirming no visual regression from either change).

Verification for this revision's correction (`disabled.click({ force: true })` → `page.mouse.click` at rendered coordinates, in `MDSwitch.browser.spec.ts` only):

- `pnpm verify --only format --files src/shared/ui/material/components/switch/MDSwitch.browser.spec.ts` — first run failed (long `page.mouse.click(...)` argument line exceeded the formatter's line-length rule); `pnpm verify --fix-only --files src/shared/ui/material/components/switch/MDSwitch.browser.spec.ts` applied the safe automatic wrap (inspected: whitespace-only, no logic change); rerun **passed**.
- `pnpm verify --only eslint --files src/shared/ui/material/components/switch/MDSwitch.browser.spec.ts` — passed.
- `pnpm verify --only type-check` — passed (40 changed files against `origin/develop`; no new type error).
- `pnpm verify --only storybook-behavior --files src/shared/ui/material/components/switch/MDSwitch.vue src/shared/ui/material/components/switch/MDSwitch.stories.ts src/shared/ui/material/components/switch/MDSwitch.browser.spec.ts` — **passed** (8/8), confirming the corrected disabled-pointer assertion still proves no selection intent/state change under ordinary (non-`force`) real pointer input.

Verification for the prior revalidation-only refresh (`2026-08-11T12:15:00.000Z`): no production, test, config, or proof file changed, so no new verifier-managed check was required. That revalidation consisted of directly reading `ARCHITECTURE.md` revision `2026-08-11T12:05:00.000Z` in full, confirming its only change was the `M3E-004` row citation, and confirming that citation against this file's "Component-owned proof" section and `MDSwitch.browser.spec.ts` (grep-confirmed the cited test name exists at that file's line 34) and against `docs/m3e-defects.md`'s `M3E-004` record (consistent, also citation-only-corrected).

Verification for this revalidation-only refresh (`2026-08-11T12:45:00.000Z`): no production, test, config, or proof file changed, so no new verifier-managed check was required. The revalidation consisted of directly reading `ARCHITECTURE.md` revision `2026-08-11T12:30:00.000Z` in full and diffing it against the prior revision (`2026-08-11T12:05:00.000Z`): the only change is the "Renderer mapping and gaps" table's `presentation` row Coverage cell (`wrapper-correction` → `not-applicable`); every other cell, section, and decision is byte-identical. Cross-checked `docs/component-adapter.md` directly (grep-confirmed at lines 30–34 and 40) to verify `not-applicable` is one of the five valid Coverage-column renderer-status enum values and that `wrapper-correction` is a separate, non-Coverage enum, confirming the correction is a genuine conformance fix and not a decision change. Confirmed this file's own "Component-owned proof", "Tokens and renderer mappings", and "Architecture deviations" sections make no reference to `wrapper-correction` or to the corrected cell's prior invalid value, so no implementation-owned content required any change.

Verification for this revalidation-only refresh (`2026-08-11T13:45:00.000Z`): no production, test, config, or proof file changed, so no new runtime-affecting verifier-managed check was required. The revalidation consisted of directly reading `ARCHITECTURE.md` revision `2026-08-11T13:30:00.000Z` in full and comparing it against the content this file was already current against (revision `2026-08-11T12:30:00.000Z`): every table row, prose section, and decision — including the `presentation` row's `not-applicable` Coverage value and the `M3E-004` row's citation — is identical; only the "Artifact revision" and "Revision summary" header fields and Markdown table column-width whitespace changed, matching `ARCHITECTURE.md`'s own revision summary. Confirmed this file's "Component-owned proof", "Tokens and renderer mappings", and prior "Architecture deviations" sections already reflect both underlying content corrections (the `M3E-004` citation and the `presentation` Coverage enum value) that predate this formatting-only architecture revision, so no implementation-owned content required any change. Ran `pnpm verify --only format --files src/shared/ui/material/components/switch/IMPLEMENTATION.md` after editing this file's own header/verification/deviation/readiness text — passed.

Verification for this revision's real-touch-input proof addition (`2026-08-14T14:00:00.000Z`, `MDSwitch.browser.spec.ts` only — no `MDSwitch.vue`, `ARCHITECTURE.md` risk mapping unchanged):

- `pnpm verify --only format --files src/shared/ui/material/components/switch/MDSwitch.browser.spec.ts src/shared/ui/material/components/switch/ARCHITECTURE.md src/shared/ui/material/components/switch/IMPLEMENTATION.md` — passed.
- `pnpm verify --only eslint --files src/shared/ui/material/components/switch/MDSwitch.browser.spec.ts` — passed.
- `pnpm verify --only type-check` — passed (24 changed files against `origin/develop`; no new type error).
- `pnpm verify --only storybook-behavior --files src/shared/ui/material/components/switch/MDSwitch.vue src/shared/ui/material/components/switch/MDSwitch.stories.ts src/shared/ui/material/components/switch/MDSwitch.browser.spec.ts` — **passed** (10/10, `pnpm test:storybook-behavior`, the storybook-behavior Playwright project — Desktop Chrome/Chromium channel with `test.use({ hasTouch: true })` on the new `touch input` describe block, real `page.touchscreen.tap` mechanism), confirming: (1) real touch on the `presentation` switch's own visible region does not mutate the renderer-owned `checked` and its `aria-hidden`/`tabindex` presentation semantics stay unchanged; (2) real touch on the `presentation-composition` decorative region produces exactly one owning-fixture action, that action's resulting state flows back into `selected`/the rendered `checked`, and the decorative renderer never independently toggles. No `MDSwitch.vue` production change was needed — the existing `pointer-events: none` suppression already generalizes correctly to touch under the installed 2.7.4 renderer. Every other test in the file (mouse, keyboard, rejected-intent, tab-order, target-hit, native-label, host-attribute-boundary) remained green in the same run.

This implementation stage did not run migration, independent review, or the outer workflow's final verification.

## Architecture deviations

The current `2.7.4` compatibility revalidation has no architecture deviation: it preserves the controlled mapping, private renderer boundary, and public contract while adding the missing native-label observable proof.

### Historical pre-2.7.4 record

None. Every implemented pass matches ARCHITECTURE.md revision `2026-08-11T13:30:00.000Z` exactly: the `beforeinput`-intent mapping, the private `onBeforeinput` typing seam, the localized `ElementInternals` shim, the owner-local browser-proof relocation, the `presentation` composition pass-through fixture, and the rejected-intent proof. The `TargetHitArea` story fix and the `scripts/lib/storybookBehaviorRisk.mjs` registry correction (an earlier revision) are both outside ARCHITECTURE.md's selected surface; neither changes any architecture decision, public API, token, or renderer mapping. The `disabled.click({ force: true })` → `page.mouse.click` proof-mechanism fix (an earlier revision) remains required by the current `ui-browser-behavior` skill's prohibition on Playwright `force`; it asserts exactly the same `disabled` non-emission/non-mutation contract ARCHITECTURE.md's "Renderer mapping and gaps" already selects. The prior revision (`2026-08-11T12:30:00.000Z`) changed only the Coverage cell of its "Renderer mapping and gaps" table's `presentation` decorative-composition row, correcting the invalid `wrapper-correction` value to the correct `not-applicable` enum value. This revision (`2026-08-11T13:30:00.000Z`) makes no content change at all — it is a formatting-only Markdown table column-width correction routed from a `pnpm verify --only format` failure. No architecture decision, public API, token, ownership, dependency closure, or renderer mapping decision changed at either step.

## Remaining blockers

None.

## Migration readiness

Ready for the unchanged migration scope. This compatibility pass changes no consumer, product, or migration-owned runtime behavior; it only revalidates the private 2.7.4 boundary and records the native-label defect result.

### Historical pre-2.7.4 record

Ready. Runtime, private typing, the Switch-owned test seam, owner-local browser/story proof — including the non-`force` disabled-pointer assertion — and every focused stage verification listed above pass and match ARCHITECTURE.md revision `2026-08-11T13:30:00.000Z`. This revision changed no implementation content and no architecture decision, so no re-verification of runtime, tests, or stories was required beyond the direct content-diff and formatting-only confirmation recorded above. Consumer inventory, migrating `SettingsSwitchListItem`/`AppUpdateSettings` from `@shared/ui/Switch` to the new canonical `MDSwitch`, legacy `src/shared/ui/Switch` removal, and product-scenario verification remain exclusively in the migration stage. Per ARCHITECTURE.md's "Migration plan" item 4, migration must directly re-verify (not assume) that both current consumers' `presentation`-only usage keeps them structurally unreachable by the corrected `beforeinput` interception path.
