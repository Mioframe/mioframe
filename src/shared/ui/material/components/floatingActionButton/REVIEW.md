# Floating action button review

Verdict: compliant-with-listed-risks
Required return family: none
Required return stage: none
Completion status: complete
Final workflow verification readiness: ready
Operator visual status: no-reported-defect
Blockers: none
Major issues: none
Minor issues: none
Accepted risks: one (see below) — local real-browser re-verification of two edited legacy/cross-owner Playwright specs was not run in this sandbox; independently confirmed low-risk by direct content inspection; exact-head CI covers it.

## Goal and scenarios reviewed

Independent review, fresh worker, no memory of any prior design/architecture/implementation/migration/review work on this family. Read in full from the workspace: root `AGENTS.md`, `src/AGENTS.md`, `src/shared/AGENTS.md`, `src/shared/ui/AGENTS.md`, `src/shared/ui/material/AGENTS.md`, `src/shared/ui/material/docs/component-workflow.md`, `src/shared/ui/material/docs/component-adapter.md`, `docs/testing/migration-plan.md` (complete), and `src/shared/ui/material/docs/m3e-defects.md` (complete). Read the complete current family artifact chain: `DESIGN.md` (status current, source checked 2026-08-14, refresh due 2026-09-13 — not due), `ARCHITECTURE.md` (status ready, renderer revision `@m3e/web@2.7.4`), `IMPLEMENTATION.md` (status complete), `MIGRATION.md` (status complete, review readiness ready).

Read production/proof source directly, not through artifact prose: `MDFab.vue`, `MDFab.test.ts` (all 12 tests, independently executed via `npx vitest run` — 12/12 pass), `MDFab.browser.spec.ts` (all 3 tests), `MDFab.visual.spec.ts` (all 4 tests) plus its 4 baseline PNGs, `MDFab.stories.ts` (all 5 story exports), `index.ts`, the root `@shared/ui/material` barrel, `m3eFab.d.ts`, `config/vueCustomElements.ts`/`.test.ts` (repo-root `config/`, not under `src`), `src/shared/ui/Button/index.ts`, `src/pages/RepoExplorer/RepoExplorerPane.vue`, and both edited legacy-proof files (`tests/e2e/storybook/focusIndicator.spec.ts`, `tests/e2e/visual/shared-ui/md-fab-family.spec.ts`) in full, plus the installed `node_modules/@m3e/web/package.json`, `dist/fab.js`, `dist/core.js`, and `dist/src/fab/FabElement.d.ts` artifacts directly.

Scenario reviewed: the single approved no-consumer library scenario — a medium-size, primary-container-color FAB with a required icon, a required accessible `label`, and native click/keyboard activation. No product consumer currently adopts `MDFab`; `RepoExplorerPane.vue`'s pre-existing `MDExtendedFab`/`FabContainer` usage is a separate, out-of-scope official family and is unaffected.

## Official design compliance

`DESIGN.md` is current and complete: identity/anatomy, variants/configurations, geometry, states/behavior, usage guidance, accessibility, and the full official token catalogue (11 token sets) are all present with an explicit source ledger. Genuine official ambiguities (the undocumented "lowered" elevation applicability, the Web Expressive "Unavailable" platform status against an installed general-Web renderer) are correctly recorded as unresolved/accepted rather than guessed. No design-contract defect found.

## Architecture compliance

`ARCHITECTURE.md` correctly applies the workflow's no-consumer default-scenario rule: it independently confirms zero product consumers of a plain FAB and selects the one unambiguous official standalone default (medium size, primary-container color) rather than building unused configurability. Every deferred surface (other sizes/colors, deprecated small/surface, `disabled`, `lowered`, Extended anatomy, link/form fields, hover/focus tooltip) is traced to an exact `DESIGN.md` section and given a valid `docs/component-adapter.md` enum value — no invalid `direct`/`partial`/`missing`/`divergent`/`not-applicable` or `implement-now`/`defer`/`wrapper-correction`/`temporary-renderer-workaround`/`m3e-fix`/`blocked`/`source-conflict` value found. The `disabled` omission is correctly classified as guidance-forbidden, not merely deferred, matching DESIGN.md's explicit "don't disable the FAB" accessibility guidance and the absence of a disabled token set.

This pass's own contribution — updating the recorded renderer revision from `2.6.3` to the now-installed `@m3e/web@2.7.4` and converting the migration plan from a forward-looking legacy-removal task to a drift-reconfirmation task — is itself independently correct: the installed lockfile-resolved version is confirmed `2.7.4` (`node_modules/@m3e/web/package.json`), and the legacy `MDFab` is confirmed already removed (see Migration and legacy removal below), so a forward-looking removal task would have been stale.

The host-attribute boundary matches `docs/component-adapter.md`'s minimum common allow-list exactly (`class`, `style`, `id`, `title`, `data-*`) with no unjustified addition. The zero-token contract is conditioned on an implementation-stage installed-artifact confirmation rather than assumed by analogy — this condition was independently re-verified against the actual installed `2.7.4` artifact (see Implementation compliance). Dependency closure (`none`) is accurate; `FabContainer.vue` is correctly classified as migration-stage inventory, not an architecture dependency, since it is generic shared UI, not an official Material family.

## Implementation compliance

`MDFab.vue` implements the architecture exactly, independently confirmed by direct read: `defineOptions({ inheritAttrs: false })`, no `v-bind="$attrs"` anywhere, an explicit `getForwardedAttrs()` allow-listing only `id`/`title`/`data-*`, with `class`/`style` merged separately in the template (`['md-fab', attrs.class]`, `:style="attrs.style"`) so the adapter-owned class always wins. The private renderer constants `rendererVariant: RendererFabVariant = 'primary-container'` and `rendererSize: RendererFabSize = 'medium'` are typed against `@m3e/web/fab`'s exported unions, are never derived from props/attrs, and are asserted in tests to survive an attempted override. `label` maps directly to `:aria-label="props.label"`; the `icon` slot renders into the renderer's default (unnamed) slot (`<slot name="icon" />` inside `m3e-fab`) with no `slot` attribute leak (independently confirmed by the test asserting `attributes('slot')` is `undefined`); a DEV-only `onMounted` + `useSlots()` check warns on an empty icon slot.

`MDFab.test.ts` contains exactly 12 `it(` blocks (independently counted and independently executed: `npx vitest run src/shared/ui/material/components/floatingActionButton/MDFab.test.ts` reports "Tests 12 passed (12)"), covering: renderer-constant immutability; `label` → `aria-label`; icon-slot rendering into the renderer's default slot; DEV-mode empty-slot warning present/absent; `click` forwarding; and the complete host-attribute allow-list/rejection matrix, including reactive add/remove/re-add of allowed attributes and rejection of `disabled`/`disabled-interactive`/`lowered`/`extended`/link/form attributes/`variant`/`size`/an unknown attribute/an undeclared listener.

`MDFab.browser.spec.ts` contains exactly 3 `test(` blocks (independently counted): accessible-name resolution plus native click; Space and Enter keyboard activation each producing exactly one `click`; and host-attribute-boundary rejection at the actual rendered custom-element state across two dynamic-update cycles (`disabled`/`extended`/`lowered`/`size`/`variant` all read directly from the live DOM element).

`MDFab.visual.spec.ts` contains exactly 4 `test(` blocks with 4 correspondingly named baseline PNGs present in `MDFab.visual.spec.ts-snapshots/` (`md-fab-states-linux.png`, `md-fab-hover-linux.png`, `md-fab-focus-linux.png`, `md-fab-pressed-linux.png`, independently confirmed by directory listing): resting appearance plus real pointer-hover, real keyboard-focus, and real pointer-press feedback, matching the Button/Checkbox/Switch `RealInteractionFeedback` precedent. `MDFab.stories.ts` contains exactly 5 story exports (`Default`, `VisualStates`, `BehaviorContracts`, `HostAttributeBoundary`, `RealInteractionFeedback`), independently confirmed by direct read.

**The zero-`--md-comp-fab-*`-token decision was independently re-verified against the installed `2.7.4` renderer artifact from scratch, not trusted from any artifact's prose.** `node_modules/@m3e/web/package.json` confirms the installed version is exactly `2.7.4`. Direct inspection of `node_modules/@m3e/web/dist/fab.js` confirms: line 325, the `primary-container` variant's `containerColor` resolves `var(--m3e-primary-container-fab-container-color, var(--m3e-fab-container-color, ${DesignToken.color.primaryContainer}))`; line 321/323, `labelTextColor`/`iconColor` resolve through `${DesignToken.color.onPrimaryContainer}`; line 327, resting `containerElevation` resolves through `${DesignToken.elevation.level3}`; line 360, hover `containerElevation` through `${DesignToken.elevation.level4}`; lines 358/373/388, hover/focus/pressed `stateLayerOpacity` resolve through `${DesignToken.state.hoverStateLayerOpacity}`/`focusStateLayerOpacity`/`pressedStateLayerOpacity`; line 47, the medium size's `shape` resolves through `${DesignToken.shape.corner.largeIncreased}`. `node_modules/@m3e/web/dist/core.js` independently confirms every one of those `DesignToken` members resolves to a public `--md-sys-*` custom property with a literal Material-spec fallback: line 2213 `primaryContainer: var(--md-sys-color-primary-container, #EADDFF)`; line 2215 `onPrimaryContainer: var(--md-sys-color-on-primary-container, #4F378B)`; line 2347/2349 `level3`/`level4`; line 2585 `largeIncreased: var(--md-sys-shape-corner-large-increased, ...)`; lines 2598/2600/2602 `focus/hover/pressedStateLayerOpacity`. `src/shared/ui/material/foundation/theme.css` (lines 70–71) and `foundation/tokens.css` (lines 19, 38, 41, 160, 167, 170) independently confirm every one of those `--md-sys-*` variables is actually declared, so the resolution chain is complete end to end with no dangling fallback-only value at the currently installed `2.7.4`. No token-resolution gap exists; the zero-token architecture decision is correct and current-version-confirmed.

`node_modules/@m3e/web/dist/src/fab/FabElement.d.ts` independently confirms `@default "primary-container"` and `@default "medium"` remain unchanged at `2.7.4`, matching the adapter's private constants and architecture's "renderer's own default already matches" claim.

`docs/m3e-defects.md` was independently re-read in full: it records no FAB entry, and its Loading Indicator/Switch/Checkbox/Button entries each show an identical `2.6.3`→`2.7.4` per-version revalidation discipline already applied to those families on the same 2026-08-14 date, consistent with this family's own revalidation. The IMPLEMENTATION.md-recorded "incidental observation" — that the installed `2.7.4` `primary-container` variant's `labelTextColor`/`iconColor` chain their `--m3e-primary-container-fab-*` custom property to themselves twice (fab.js lines 321/323) instead of to the documented generic `--m3e-fab-*` property, unlike every other variant — was independently confirmed accurate by direct read of those exact lines, and its non-blocking classification is correct: no override is set anywhere in `MDFab.vue`, so the literal `DesignToken.color.onPrimaryContainer` fallback (independently confirmed correct above) is what actually resolves; per `docs/m3e-defects.md`'s inclusion boundary this is "deferred official surface with no selected scenario," not a defect requiring a new `M3E-*` record.

`m3e-fab` is independently confirmed registered in `config/vueCustomElements.ts` (repo-root `config/`, not `src/shared/ui/material/config/` — the exact path differs from IMPLEMENTATION.md's shorthand reference but the registration and its `config/vueCustomElements.test.ts` coverage, `expect(isM3eCustomElement('m3e-fab')).toBe(true)`, are both confirmed present). `m3eFab.d.ts` scopes the Vue custom-element typing to exactly `variant`/`size`/`onClick` plus `HTMLAttributes`/`PublicProps` — no speculative renderer surface leaks into the public typing.

## Migration and legacy removal

Independently re-ran the architecture-stage consumer grep across `src/pages`, `src/widgets`, `src/features`, `src/entities`, and `src` broadly: zero matches for `MDFab` anywhere in product code; the only repository-wide `MDFab` references are this family's own files, the root `@shared/ui/material` barrel re-export, and two illustrative doc-comment mentions in `FabContainer.vue`/`FabContainer.stories.ts` (not imports). `RepoExplorerPane.vue` was independently re-read and confirmed unchanged — line 14 imports `FabContainer, MDExtendedFab` from `@shared/ui/Button`; lines 194–196 render only those, not the canonical `MDFab`.

Legacy `src/shared/ui/Button/MDFab.vue`/`.test.ts`/`.stories.ts` are confirmed absent by directory listing (`src/shared/ui/Button/` contains no `MDFab*` files); `src/shared/ui/Button/index.ts` was independently read in full and exports exactly `FabContainer`, `MDExtendedFab`, `MDIconButton` — no `MDFab` line.

Both cross-owner legacy-proof files were independently re-read in full, not trusted from any artifact's prose:

- `tests/e2e/storybook/focusIndicator.spec.ts`: exactly three surviving `test(` blocks, all titled for `MDButton`, `MDIconButton`, and `MDExtendedFab`; no `MDFab`-only test remains.
- `tests/e2e/visual/shared-ui/md-fab-family.spec.ts`: exactly twelve surviving `test(` blocks, every one titled `MDExtendedFab ...`; no plain-`MDFab` test remains. Its snapshot directory (`md-fab-family.spec.ts-snapshots/`) contains exactly the two `MDExtendedFab` baselines (`md-extended-fab-states-linux.png`, `md-extended-fab-interaction-states-linux.png`) and no orphaned legacy-`MDFab` baseline.

`docs/testing/migration-plan.md` was independently re-read in full: its S2-D migration-group entry and the historical note attached to the `md-button-family.spec.ts` inventory entry both carry an accurate, current historical note that the legacy plain `MDFab` no longer exists, removed by this family's own migration, with a pointer to `MIGRATION.md` — no dangling reference to a deleted component remains.

No compatibility alias was kept for the removed legacy `MDFab`, correctly, since it had zero product consumers.

## Proof and stage verification

Test counts and content were independently re-verified by direct file inspection and direct execution, not trusted from any artifact's prose: `MDFab.test.ts` has exactly 12 `it(` blocks and was independently re-run this review (`npx vitest run`), passing 12/12; `MDFab.browser.spec.ts` has exactly 3 `test(` blocks; `MDFab.visual.spec.ts` has exactly 4 `test(` blocks with 4 matching baselines — all matching `IMPLEMENTATION.md`'s claimed counts.

Owner-local `MDFab.browser.spec.ts`/`MDFab.visual.spec.ts` placement is correctly authorized: `docs/testing/migration-plan.md`'s current executable state confirms mixed discovery (legacy central plus owner-local `src/**/*.browser.spec.ts`/`*.visual.spec.ts`) is executable, and explicitly authorizes "a canonical Material family migration... to establish final owner-local Storybook browser/visual ownership in the same family workflow." This is not a still-transitional or central-registry exception.

`IMPLEMENTATION.md` reports focused `type-check`, `unit-tests` (12/12), `eslint`, `format`, and `storybook-build` passing for the FAB family's own owned files, plus direct installed-artifact re-inspection for the token-resolution and renderer-mapping revalidation against `2.7.4`. This is the family's complete required proof per `ARCHITECTURE.md`'s TEST IMPACT, and it is complete with no gap. This review independently re-confirmed the unit-test count and pass result by direct execution, and independently re-confirmed the token-resolution chain by direct artifact inspection (see Implementation compliance) rather than accepting either claim on trust.

**One genuine, narrow, already-bounded gap exists, independently re-assessed rather than accepted on any prior worker's word:** the podman-backed `storybook-behavior`/`visual` Playwright lanes for the two edited legacy/cross-owner files (`focusIndicator.spec.ts`, `md-fab-family.spec.ts`) were not re-run in this (or the current invocation's implementation/migration) sandbox — this sandbox cannot run podman-backed verify commands, and no attempt was made to work around that restriction. This review independently confirmed by direct full-file read (not diff inspection, since this sandbox does not depend on Git state) that: (a) both files' surviving content is exactly and exclusively `MDButton`/`MDIconButton`/`MDExtendedFab` proof, with zero `MDFab`-shaped assertion or fixture remaining; (b) this exact same gap, against this exact same file content, was already independently assessed and accepted by a prior independent review of this family, which additionally confirmed via diff that the edits removing the legacy `MDFab` content were pure subtractions with zero new or modified assertion logic; (c) no edit was made to either file in the current invocation's fresh implementation or migration pass (both explicitly recorded "no production, consumer, or legacy-proof edit was required this pass," independently corroborated by this review's own direct read finding no `MDFab` residue). Given this — unchanged, previously-diff-verified-as-pure-subtraction content, with `type-check`/`eslint`/`format`/`storybook-build` already exercised on these exact files by an earlier pass — the residual risk that the podman-backed real-browser lanes would newly fail on these exact files remains very low, and exact-head GitHub CI will run both lanes on the PR regardless. This is recorded as an accepted risk with an explicit required operator/architect action, not a route back to migration: the underlying edit is correct, complete, and unchanged since its own independent review; only its local real-browser re-confirmation is outstanding, and it is bounded to exactly two named files with no ambiguity about what needs to run.

## Blockers

None.

## Major issues

None.

## Minor issues

None.

## Accepted risks

- The podman-backed `storybook-behavior` lane for `tests/e2e/storybook/focusIndicator.spec.ts` and the podman-backed `visual` lane for `tests/e2e/visual/shared-ui/md-fab-family.spec.ts` were not re-run in this (or any prior) sandbox in the current invocation. Independently confirmed by direct full-file read that both files contain zero remaining `MDFab`-shaped content (only `MDButton`/`MDIconButton`/`MDExtendedFab` proof survives), that neither file was edited in the current invocation, and that this identical gap against this identical content was already independently assessed and accepted by a prior independent review (which additionally confirmed via diff that the underlying legacy-removal edits were pure subtractions). Required action: the architect should run `pnpm verify --only storybook-behavior --files tests/e2e/storybook/focusIndicator.spec.ts` and `pnpm verify --only visual --files tests/e2e/visual/shared-ui/md-fab-family.spec.ts` via a podman-capable environment before or alongside opening the PR, or treat the exact-head GitHub CI run of these same lanes as satisfying this confirmation before merge. Do not skip both.
- Renderer-version-drift revalidation for this family is now current only as of `@m3e/web@2.7.4` (independently reconfirmed by this review). Any future lockfile bump to a newer `@m3e/web` version requires a fresh implementation-stage revalidation of the token-resolution chain and renderer-mapping table before treating this family's zero-token/direct-mapping decisions as current-version-confirmed again, consistent with the same per-version discipline `docs/m3e-defects.md` already applies to Loading Indicator, Switch, Checkbox, and Button. This is an ordinary forward-looking maintenance expectation, not a present defect.

## Items not required

- No FAB (regular)/large size, no non-default color, no deprecated small/surface variant — no confirmed scenario requires them; correctly deferred.
- No `disabled`/`disabled-interactive` prop — official guidance forbids disabling a FAB; correctly omitted, not merely deferred.
- No `lowered` elevation — official source does not resolve when it applies; correctly left unimplemented pending a future source resolution, not guessed.
- No Extended FAB or FAB menu behavior inside this family — both are separate official families, correctly out of scope; `RepoExplorerPane.vue`'s existing `MDExtendedFab` usage is untouched.
- No link/form (`href`/`download`/`target`/`rel`/`name`/`value`/`type`) surface — renderer supports it, no confirmed scenario selects it.
- No web hover/focus tooltip — documented "should" guidance, but no canonical Material Tooltip family exists yet to compose as a dependency; correctly deferred rather than faked.
- No `--md-comp-fab-*` public token — no confirmed contextual-override scenario; default appearance resolves entirely from already-public foundation tokens, independently confirmed above against the currently installed `2.7.4` artifact.
- No new `M3E-*` defect record — the one incidental self-chaining CSS-variable observation in the installed `2.7.4` `primary-container` variant does not meet `docs/m3e-defects.md`'s inclusion boundary for this family's selected (zero-override) contract.
- No fabricated product consumer — the explicit no-consumer library-only scenario is honestly recorded, not worked around with an invented integration.

## Routing evidence

No architecture, implementation, or migration defect was found. Every artifact claim independently re-verified in this review from primary sources rather than accepted on any prior worker's word: installed renderer version (`2.7.4`, `node_modules/@m3e/web/package.json`), the complete zero-token resolution chain (`dist/fab.js`, `dist/core.js`, `foundation/theme.css`, `foundation/tokens.css`), the renderer-default confirmation (`dist/src/fab/FabElement.d.ts`), exact test counts and an independent live execution of the unit suite (12/12 passing), `m3e-fab` custom-element registration (`config/vueCustomElements.ts`/`.test.ts`), the consumer-inventory grep (zero product consumers), the exact surviving content of both edited legacy-proof files by direct full-file read, and the legacy `MDFab` barrel/file removal. Every check matched the documented record exactly, with one path correction noted (the `vueCustomElements.ts` config lives at repo-root `config/`, not under `src/shared/ui/material/config/` as IMPLEMENTATION.md's shorthand reference implied — this is a citation-path nit, not a substantive defect, since the registration and its test coverage are both independently confirmed present). The single accepted risk above is a bounded, independently-assessed-low-risk, sandbox-caused local-execution gap on non-family-owned blast-radius proof that has not changed since a prior independent review already accepted it, not a content or design defect in this family's own work — it does not require returning to `migration` or any earlier stage. Route is `none/none`; the family is ready for the outer orchestrator's final workflow verification and architect-owned PR/CI, contingent on the one listed operator action being completed (locally or via CI) before merge.
