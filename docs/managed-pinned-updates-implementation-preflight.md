# Managed pinned application updates — implementation preflight

This file is retained only as a compatibility pointer because release documentation and PR history link to it. It defines no requirements or implementation status of its own.

The single canonical architecture and implementation contract is [`managed-pinned-updates.md`](./managed-pinned-updates.md).
