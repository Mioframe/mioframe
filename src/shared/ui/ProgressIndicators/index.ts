// Real implementation relocated to `@shared/ui/material/components/progress-indicator` (the
// canonical Material "Progress indicators" component family). This module forwards only, so
// existing consumers outside Material keep working without a parallel implementation.
export { MDCircularProgressIndicator } from '@shared/ui/material/components/progress-indicator';
