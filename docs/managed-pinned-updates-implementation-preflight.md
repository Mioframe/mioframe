# Managed pinned application updates — implementation preflight

This file is retained only as a stable link from release documentation and PR history. It contains no independent architecture contract and no historical stage status.

The single canonical architecture contract and current implementation status are defined in [`managed-pinned-updates.md`](./managed-pinned-updates.md).

Current PR 169 status:

- the managed-update core is implemented;
- recovery architecture decisions are resolved;
- the recovery runtime and required proof are not yet implemented;
- the PR remains draft and must not merge until recovery implementation, proof, final verification, complete review, and operator acceptance are complete.

Historical stage-preflight documents were removed because they described intermediate implementation checkpoints and could contradict the current whole-PR status.
