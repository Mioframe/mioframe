---
name: material-library-next
description: 'Use when the user wants Material library implementation to continue without naming a family.'
---

# Material library next

Read `src/shared/ui/material/docs/roadmap.md` and select exactly its one recorded next family or blocker.

- Do not consult or create a separate inventory, registry, queue, audit, checklist, or workflow.
- Confirm the selected family against current repository evidence.
- When the roadmap names an executable family, invoke `material-component <family>` and let that skill execute the complete staged workflow.
- When the roadmap names a blocker, report that exact blocker instead of selecting another family.
- Complete exactly one family per task or PR.
- Do not pre-plan later families while the selected family remains active.