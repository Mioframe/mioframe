# Virtual collection API — moved

The canonical documentation for the shared virtualization library now lives beside its implementation:

[`src/shared/ui/virtualization/README.md`](../src/shared/ui/virtualization/README.md)

This file is retained only as a compatibility pointer for existing architecture records and links created during the database virtualization capability work. New documentation and references should link directly to the colocated README.

Database-specific architecture, browser proof, result records, and performance planning remain under `docs/` because they belong to the database migration rather than to the reusable shared library.
